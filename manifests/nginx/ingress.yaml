apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-http
spec:
  ingressClassName: nginx

  tls:
    - hosts:
        - nginx.3-85-74-102.nip.io
      secretName: nginx-tls

  rules:
    - host: nginx.3-85-74-102.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nginx
                port:
                  number: 80